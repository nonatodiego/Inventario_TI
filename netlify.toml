[build]
  command = "npm run build"
  publish = "dist"

# Proxy /api/* calls to your backend URL on Render (replace placeholder)
[[redirects]]
  from = "/api/*"
  to = "https://inventario-ti.onrender.com/api/:splat"
  status = 200
